<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
  <meta>
    <sampleQuery>select * from {table} where a='cat' and b='dog';</sampleQuery>
  </meta>
  <bindings>
    <select itemPath="" produces="XML">
      <urls>
        <url>http://fake.url/{a}</url>
      </urls>
      <inputs>
        <key id='id' type='xs:string' paramType='path' required="true" />
        <key id='b' type='xs:string' paramType='variable' required="true" />
      </inputs>
      <execute><![CDATA[          
          var urlBasica =  "www.carrosaojose.com.br/veiculos?revenda_id=3639";

      		// Create an instance of the request object
      		var request_instance = y.rest(urlBasica);

      		// Make request to web service with HTTP method 'GET'
      		var returned_response = request_instance.get();

          var data = returned_response;
          var xpath = "//div[contains(@class, 'titulo')]";
          var xdata = y.xpath(data,xpath);

      		var teste = {}
      		teste.url =  returned_response.response();
      		teste.data = xdata;

          response.object = y.jsonToXml(teste);

      ]]></execute>    
    </select>  
  </bindings>
</table>