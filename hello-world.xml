<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
    <meta>
        <author>me</author>
        <description>carros</description>
    </meta>
      <bindings>
        <select produces="XML">
        <urls>
          <url>http://www.carrosaojose.com.br/veiculos</url>
        </urls>
        <inputs>
          <key id="revenda_id" as="revenda" type="xs:string" paramType="query" required="true" />
        </inputs>

          <execute><![CDATA[

          var resultado = {};
          var xpath = "//div[contains(@class, 'produto')]";
          var url = "http://www.carrosaojose.com.br/veiculos?revenda_id=3639";
          var query   = y.query("SELECT * FROM html WHERE url=@url AND xpath=@xpath", {url: url, xpath: xpath});
          y.log(query);
          resultado.online = true;

          response.object = y.jsonToXml(resultado);

 ]]></execute>
      </select>
      </bindings>
</table>