<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
    <meta>
        <author>me</author>
        <description>carros</description>
    </meta>
      <bindings>
        <select produces="XML">
        <urls>
          <url>https://rawgit.com/teles/yql-tables/master/exemplo.html</url>
        </urls>
        <execute><![CDATA[
          //var urlParaRaspar = "https://cdn.rawgit.com/teles/yql-tables/master/html4.html";
          var urlParaRaspar = "http://www.carrosaojose.com.br/veiculos?revenda_id=3639";
          //var data = y.query("select * from html where url='https://cdn.rawgit.com/teles/yql-tables/master/html4.html'");

          var data = y.rest(urlParaRaspar).accept('text/html').get().response;    
          var responseData = y.xpath(data, "//div[@class='valor']");
          y.log(responseData);
          
          var tidificado = y.tidy(responseData);
          y.log(tidificado);

          var jsonficado = y.xmlToJson(tidificado);
          y.log(jsonficado);

          response.object = tidificado;



        ]]>
        </execute>
      </select>
      </bindings>
</table>