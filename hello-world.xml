<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
  <meta>
    <sampleQuery>select * from {table} where a='cat' and b='dog';</sampleQuery>
  </meta>
  <bindings>
    <select itemPath="" produces="XML">
      <urls>
        <url>http://fake.url/{a}</url>
      </urls>
      <inputs>
        <key id='id' type='xs:string' paramType='path' required="true" />
      </inputs>
      <execute><![CDATA[                
          var urlBasica =  "www.carrosaojose.com.br/veiculos?revenda_id=3639";
          var resultado = {};
          resultado.id = "nada";

          var x = y.rest(urlBasica).accept("text/html").get().response;
          var livedata = y.xpath(x, "//div[contains(@class, 'titulo')]");

          resultado.livedata = livedata;          

          response.object = y.jsonToXml(resultado);


      ]]></execute>    
    </select>  
  </bindings>
</table>